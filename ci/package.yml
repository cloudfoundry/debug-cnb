---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
- name: debug-buildpack

outputs:
- name: artifactory

caches:
- path: go-module-cache

run:
  path: debug-buildpack/ci/package.sh
